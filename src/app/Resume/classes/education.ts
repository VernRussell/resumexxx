export class Education {

  constructor (public name: string, public school: string, public city: string, 
  public graduated: string, public dates: string, public degree: string,
  public technologies: string[], public year: string, public mode: string ) {
    
  }

}