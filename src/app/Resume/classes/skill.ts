export class Skill {
  
  constructor (public months: number, public tasks: string[] ) {
  }
}

// Fill from Technology array, put in category
// From the technologies listed on my positions, add in any that are missing, use category of 'missing'
// Debating on if I should list the months worked within 
